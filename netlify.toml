# App Vite + React. O Netlify roda o build e publica a pasta "dist".
[build]
  command = "npm run build"
  publish = "dist"

# SPA: qualquer rota cai no index.html (evita 404 em refresh de subrotas futuras)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
